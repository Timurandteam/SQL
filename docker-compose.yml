version: '3.7'
services:
  mysql:
    image: mysql:8.0.18
    restart: always
    ports:
      - '3306:3306'
    volumes:
      - ./schemes:/docker-entrypoint-initdb.d
    environment:
      - MYSQL_DB=app
      - MYSQL_USER=app
      - MYSQL_PASSWORD=pass